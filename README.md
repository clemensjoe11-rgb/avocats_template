# MEYNIOGLU LAW – Admin & Booking

## Start
```bash
npm i
# optional: set env vars ADMIN_USER, ADMIN_PASS, SESSION_SECRET, PORT
npm start
# open http://localhost:3000/admin
```
Admin-Login per Session + CSRF-Header `X-CSRF-Token`.
Daten werden in `db.json` gespeichert (Demo).